
MODULE(m1);

PROBLEM;

q: PROC (f FIXED IDENT);
   f := f +1 ;
END;

a: TASK;
   DCL h INV FIXED INIT(1);
   CALL q(h);
/*$       ^
error: q:parameter: pass INV data as non INV IDENT parameter
*/ 
END;


MODEND;
