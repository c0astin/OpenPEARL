
MODULE(m1);

PROBLEM;

q: PROC (f FIXED IDENT);
   f := f +1 ;
END;

a: TASK;
   DCL h INV FIXED INIT(1);
   CALL q(h);
/*$       ^
error: q: type mismatch: formal parameter FIXED(31) IDENT --- got INV FIXED(31)
*/ 
END;


MODEND;
