this folder contains tests for expected error messages from the compilation

The tests are ordered according the coresponding chapters in the language report

Tests, which are known to fail and a ticket exists should be
renamed with the ticket number as additional extension.

Only test with a name with 3 leading digits and extension '.prl' 
are recognized.


