MODULE(refChar);

PROBLEM;

DCL rc REF CHAR();

proc: PROC RETURNS(REF CHAR());
/*$                ^
error: PROC: RETURNS: type REF CHAR() is not allowed as result type
*/
   RETURN(rc);
END;


MODEND;

  
